# Required for all demo commands.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides.
OPENAI_MODEL=gpt-5.4-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional local directory for demos that persist memory.
MEMORY_SAMPLE_DATA_DIR=.memory_sample_data
